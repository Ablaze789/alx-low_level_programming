#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 *	main - This the entry point for the programme
 *	@argc: The length of the array argv
 *	@argv: An array of the strings passed into the command line
 *	Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
