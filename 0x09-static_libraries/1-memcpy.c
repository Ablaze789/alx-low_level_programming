#include "main.h"

/**
 *	_memcpy - a function that prints a string, followed by a new line.
 *	@dest: string to print
 *	@src: string to print
 *	@n: string to print
 *	Return: char
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (0);
}
