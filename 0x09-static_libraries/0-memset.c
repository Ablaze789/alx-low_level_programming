#include "main.h"

/**
 *	_memset - a function that prints a string, followed by a new line.
 *	@c: string to print
 *	Return: void
 */

int char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
