#include "main.h"

/**
 *	_strlen - This prints the length of a string
 *	@s: This holds the address of the string
 *	Return: Always 0
 */

int _strlen(char *s)
{
	int len = strlen(s);
	return (len);
}
