#include "main.h"

/**
 *	_puts - This is a funtion that prints a string,
 *	followed by a new line, to stdout
 *	@str: String to be printed to stdout
 *	Return: Always 0
 */

void _puts(char *str)
{
	puts(str);
}
