#include <stdio.h>

/**
 * 	main - Entry point
 *
 * 	Description: 'This prints the size of different data type'
 *
 * 	Return: Always 0 (Sucess)
 */

int main(void)
{
	sizeof(char);
	sizeof(int);
	sizeof(long int);
	sizeof(long long int);
	sizeof(float);
	return (0);
}
